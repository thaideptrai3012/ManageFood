<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.managefood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ManageFood">
        <activity android:name=".UpdateFoodActivity"></activity>
        <activity android:name=".ListFoodActivity" />
        <activity android:name=".AddFoodActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".ChangePassActivity" />
        <activity android:name=".dialog_forgot" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".FoodDetailActivity" />
        <activity android:name=".FoodActivity" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>